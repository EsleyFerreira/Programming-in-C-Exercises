#include <stdio.h>

int main() {
    // Declaração e inicialização das variáveis
    int num1 = 2, num2 = 5;
    float NUM = 3;

    // Atualiza o valor da variável NUM usando uma operação matemática
    NUM = NUM + num1 / num2;

    // Exibe o valor da variável NUM
    printf("O valor de NUM é: %f", NUM);

   
    return 0;
}